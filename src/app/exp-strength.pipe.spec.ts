import { ExpStrengthPipe } from './exp-strength.pipe';

describe('ExpStrengthPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpStrengthPipe();
    expect(pipe).toBeTruthy();
  });
});
